%div.row
  %div.card
    %h1= t('emitters.new_subtitle')
    = form_for :emitter, :url => { :action => "update"}, method: 'PUT', :html => {:class => "form row"} do |f|
      = render partial: 'form', locals: {f: f}